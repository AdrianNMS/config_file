spring:
  config:
    location:
      optional:
        common.yml
  thymeleaf:
    reactive:
      max-chunk-size:
        1024
      chunked-mode-view-names:
        listar-chunked,*chunked*
server:
  port:
    8081